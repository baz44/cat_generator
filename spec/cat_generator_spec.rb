require 'spec_helper'

describe CatGenerator do
  it 'has a version number' do
    expect(CatGenerator::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(true).to eq(true)
  end
end
